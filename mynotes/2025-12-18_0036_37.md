go through project structure, see if there are inappropriate files/folders being added to git, but should've been in gitignore

then do a thorough check of the project because I want to deploy it to vercel now

then git commit and push changes (group each related features)
