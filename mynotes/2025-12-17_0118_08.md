## yes, a few things to adjust

when showing the contribution schedule,

I see we can set the frontload year, that's all good but I want that year's value to be adjusted if I manually update previous or future years

ie. Let's say I set frontload year for current child in 2028

let's say I click on "Generate Optimal Schedule" button,

it shows 2028, I need to frontload $15K

it puts $5000 in year 2025, but I already contributed and I didn't contribute that much for current child in year 2025, so I manually enter the amount I did (let's say it's $1000), then save it.

now, that $15K should be larger now since I changed the 2025 value from $5000 to $1000.

---

also I see if I enter only expect year income in Settings, the ACESG and CLB are calculated,

ie, if I have only 2025 and 2026 entered, then only those two rows for those two cols are computed.

I want it to be treated as $0 income for the years I didn't enter, so it'll assume I have no income and thus give max. And if I want to enter a value for the year's expected income, then it'll adjust to that instead of $0.

Let's make the default behaviour like this.

---

also where is the chart for the children when viewing a plan? it should be at the top, with ability to toggle on/off each child from the chart/graph
